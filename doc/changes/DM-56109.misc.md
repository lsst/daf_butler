Removed the unreachable ``config`` parameter from the ``StorageClassFactory`` constructor.

``StorageClassFactory`` uses the ``Singleton`` metaclass, whose ``__call__`` accepts no arguments, so ``StorageClassFactory(config)`` has always raised ``TypeError``.
Use ``StorageClassFactory().addFromConfig()`` to add definitions, as all existing callers already do.
