The datastore tests that used a moto-mocked S3 bucket now use the ``remote-test`` URI scheme from ``lsst.resources``.
This tests the same not-local code paths far more quickly, and ``s3fs`` is no longer required to run the test suite.
