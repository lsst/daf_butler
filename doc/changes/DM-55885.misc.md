Test repositories with identical configurations are now built once and copied for subsequent requests, and storage class definitions derived from an identical configuration are reused rather than re-derived.
Together with a faster configuration merge and copy, this reduces test suite CPU time by about 25 per cent.
