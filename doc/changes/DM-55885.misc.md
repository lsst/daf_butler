Test repositories with identical configurations are now built once and copied for subsequent requests, rather than being rebuilt from scratch every time.
This reduces repository setup in the test suite from 151s to 88s, cutting total test suite CPU time by about 17%.
