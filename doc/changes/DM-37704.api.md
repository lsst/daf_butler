- `DatasetRef` constructor now requires `run` argument in all cases and always constructs a resolved reference.
- Methods `DatasetRef.resolved()`, `DatasetRef.unresolved()`, and `DatasetRef.getCheckedId()` were removed.
