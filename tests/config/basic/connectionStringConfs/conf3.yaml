# test that combinations of 'db' keys and explicit keys work (f.e. port)
registry:
  cls: lsst.daf.butler.registries.postgresqlRegistry.PostgreSqlRegistry
  db: 'postgresql://lsst-db.ncsa.illinois.edu:3307'
  database: 'conf3'
  expected: 'postgresql://boris:natasha@lsst-db.ncsa.illinois.edu:3307/conf3'
